"use client";

const Subscription = () => {
  return (
    <div className="flex flex-col items-start gap-2 border p-4 rounded-lg">
      <h3 className="font-extrabold text-xl">Předplatné</h3>
      {/*<p className="text-muted-foreground">{data?.user?.email}</p>*/}
    </div>
  );
};

export default Subscription;
